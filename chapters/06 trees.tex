\chapter{Syntax trees}

\epigraph{half living,\\
half leaving,\\
half outward,\\
half injured\\[0.5em]
quartered\\
section\\
of parcelled\\
land}{Bertrand Bickersteth, \enquote{Soil}}

\begin{tblsframedsymbol}{Learning objectives}{bulb}
\begin{itemize}[noitemsep, leftmargin=*]
    \item Read and interpret syntax trees to understand sentence structure
    \item Identify heads, dependents, and their functional relationships in tree diagrams
    \item Construct syntax trees for simple and complex phrases using top-down and bottom-up approaches
    \item Apply constituency tests (coordination, pro-form replacement, ellipsis, preposing) to identify phrase boundaries
    \item Use syntax trees to disambiguate structurally ambiguous sentences
    \item Recognize the pedagogical value of visual representation in grammar analysis
\end{itemize}
\end{tblsframedsymbol}

\clearpage\noindent
\section{Introduction}
\is{syntax tree|(}

In previous chapters, we've explored the building blocks of English grammar: words, phrases, and their functions. Now, we'll introduce a useful tool for visualizing how these elements come together to form sentences: syntax trees.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.4\textwidth]{figures/itisatree.pdf}
  \caption{A \term{syntax tree}}
  \label{fig:tree1}
\end{figure}

Syntax trees are diagrams that represent the hierarchical structure of sentences, showing how words combine into phrases, and phrases into larger units. While you likely won't use these diagrams directly in your ESL classroom, understanding them will deepen your grasp of English syntax and sharpen your ability to analyze language structures.


This chapter guides you through the basic principles of syntax trees. It will show you how to read and construct these diagrams, along with ways that trees can clarify ambiguous sentences. And it will lay out the pedagogical value of syntax trees for language teachers.

By the end of this chapter, you'll have a new perspective on sentence structure, one that will enhance your understanding of English grammar and, in turn, your effectiveness as an ESL teacher. Let's begin by exploring what these trees look like and why they're useful.




% the commands such as \Head don't seem to be working, so I'll include a PDF
%\begin{forest}
%where n children=0{% for each terminal node
%    font=\itshape, 			% italics
%    tier=word          			% align at the "word" tier (bottom)
%  }{%								% no false conditions, so empty
%  },
%[Clause
%	[Section ubj{NP}[\Head{N\textsubscript{\term{pro}}}[it]]]
%	[\Head{VP}
%		[\Head{V\textsubscript{\term{aux}}}[is]]
%		[\Comp{NP}
%			[\Det{DP}[\Head{D}[a]]]
%			[\Head{N}[tree]]
%		]
%	]
%]
%\end{forest}

\section{A first tree}
\is{syntax tree!how to read|(}

Figure~\ref{fig:tree1} is a \term{syntax tree}. Starting at the bottom right, it tells you that there is a word \mention{tree}. Follow that branch up and you'll see Head:N, which means that this is a noun functioning as the head of something. If you follow that next branch up, you can see that it's the head of a noun phrase (NP). Back down at the bottom, the second-to-last word is \mention{a}, which is labeled Head:D. D is short for determinative (the category). Following its branch upwards, we see it's the head of a determinative phrase (DP). This determinative phrase functions as the determiner (Det) for the NP with the head noun \mention{tree}.

Moving to the left, we encounter the verb \mention{is}, labeled as a verb with the subscript \term{aux}, indicating its special categorization as an auxiliary verb (one of the small group that has the NICER properties; see Section~\ref{sec:aux}). This verb functions as the head of the verb phrase (VP). The VP also contains a complement (Comp), which is the noun phrase we discussed earlier (with the head noun \mention{tree}).

Further to the left, we have the pronoun \mention{it}. This word is labeled as a noun with the subscript \term{pro}, indicating it's a pronoun. Following its branch up, we find it's labeled as the head of an NP, and this NP functions as the subject (Subj) of the clause.

The entire structure is labeled as a clause, which is the highest level of this tree. This clause contains both a subject (the NP with the pronoun \mention{it}) and a predicate (the VP with the auxiliary verb \mention{is} and its complement).

\begin{tblsframedsymbol}{Check your understanding}{test}
Practice reading the tree: Starting from the word \mention{tree}, trace upward and identify: (1) its category, (2) its function, (3) the phrase it heads, and (4) that phrase's function in the larger structure. Then do the same for \mention{is} and \mention{it}.
\end{tblsframedsymbol}
\is{syntax tree!how to read|)}

\section{Syntax trees, a solar-system metaphor}\label{sec:trees}
\is{head (function)|(}\is{dependent (Dep)|(}\is{complement, complementation|(}\is{modification, modifier|(}\is{object (Obj)|(}\is{subject (Subj)!|(}

Consider the solar system as a metaphor to understand categories and functions in language, particularly the functions of \mention{head} and \mention{dependent}. In the solar system, the central entity is the Sun, which serves as the \enquote{head} of the system. Everything else, from planets to dust, depends on the Sun, making them \enquote{dependents.} We can draw this as the tree in Figure~\ref{fig:solarsys}.

\begin{figure}
  \centering
  \includegraphics{figures/solarsys.pdf}
  \caption{A partial tree of the solar system}
  \label{fig:solarsys}
\end{figure}

Consider the categories and functions in the solar system:
\begin{itemize}[noitemsep]
    \item The Sun belongs to the \term{star} category. This star functions as the head of the \term{solar system}.
    \item Jupiter, Earth, Venus, etc. belong to the \term{planet} category. Each of these planets functions as the head of a \term{planetary system} (PlanSys), and those planetary systems are dependents (Dep) in the solar system.
    \item Within each planetary system:
    \begin{itemize}[noitemsep]
        \item The planet itself, such as Earth or Jupiter, is the head.
        \item Satellites around the planets are categorized as \term{Moon} or \term{Ring}. These function as dependents in their respective planetary systems.
    \end{itemize}
\end{itemize}


\begin{figure}[ht]
  \centering
  \includegraphics[width=0.4\textwidth]{figures/dogdinner.pdf}
  \caption{Another syntax tree}
  \label{fig:tree2}
\end{figure}

This hierarchical structure of heads and dependents isn't limited to the solar system. For instance, our solar system is a dependent within the larger \term{galaxy}, which has its own head: a \term{supermassive black hole}.

Applying this metaphor to language, consider a verb phrase. Just as the Sun is the head of the solar system, the verb (e.g., \mention{give}) is the head of a verb phrase. Without the verb \mention{give}, the connection between \mention{the dog} and \mention{his dinner} is lost. Both \mention{the dog} and \mention{his dinner} are dependents in the VP. Specifically, they are objects, a sub-type of complement, which, in turn, is a kind of dependent. The tree is shown in Figure~\ref{fig:tree2}.


Breaking it down further:
\begin{itemize}[noitemsep]
    \item \mention{Give} is the head of the VP.
    \item \mention{The dog} is an NP functioning as an object.
    \item Within this NP, the noun \mention{dog} is the head, and the determinative \mention{the} is the determiner.
    \item \mention{His dinner} is another noun phrase object. Here, the noun \mention{dinner} is the head, and the pronoun \mention{his} is the determiner.
\end{itemize}

This structure showcases how different phrases in language have heads and dependents, similar to the entities in our solar system.

\begin{tblsframedsymbol}{Practice: applying the metaphor}{pencil}
Using the solar system metaphor, analyze this sentence: \mention{The students read difficult books}. Identify: (1) What is the \enquote{sun} (head) of the whole sentence? (2) What are the \enquote{planets} (major dependents)? (3) Within \mention{difficult books}, what is the head and what is the dependent?
\end{tblsframedsymbol}
\is{head (function)|)}\is{dependent (Dep)|)}\is{complement, complementation|)}\is{modification, modifier|)}\is{object (Obj)|)}\is{subject (Subj)!|)}

\section{Why syntax trees?} \label{sec:why-trees}

Syntax trees are visual models of the relations between words and phrases in larger constructions. They are common in linguistics, but they are rarely used in language teaching. To be clear, I'm not employing trees in this book so that you can use them in your teaching. While a particularly academically oriented student may find trees useful in understanding the structure of sentences, the level of detail involved is generally not a level that students need.

So, why employ trees?

\begin{enumerate}[noitemsep]
    \item Better learning through dual-encoding: Syntax trees provide a visual representation of the relations between words and phrases in larger constructions. There is good evidence that multi-modal learning is more effective than learning that takes place in a single modality \citep{ginns2005}. And so, it's likely that by employing syntax trees alongside other methods, you will be able to recall the ideas of phrase structures, categories, and functions.
    \item Brevity and clarity: As you can see above, it takes a paragraph or more to describe the information set out in a simple syntax tree, and the explanation is often much harder to understand.
    \item Explicitness: When you draw a tree, you take an explicit position on the categories of words and phrases, along with their functional relationships. This is much more difficult in prose, which means that prose descriptions tend to be vague. As a result, it's much easier in prose to fool yourself into thinking that you've understood when you haven't. With trees, it is obvious when the analysis is correct.
    \item Practice with explicit feedback: There is very strong evidence that mastery is achieved through deliberate practice \citep{ericsson1993}. Simply put, deliberate learning involves the following cycle: attempt $\rightarrow$ explicit feedback $\rightarrow$ reflection $\rightarrow$ attempt. Employing trees allows you to easily get quick, accurate feedback by comparing trees you draw to the answer key.
\end{enumerate}

\noindent In short, we are employing trees because they are an extremely effective tool for learning syntax and for demonstrating that learning.


\section{Composing your trees}

\subsection{Marking specific dependents}\label{sec:MardDeps}

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.4\textwidth]{figures/whohelpedyou.pdf}
  \caption{Tree for \mention{Who helped you?}}
  \label{fig:tree3}
\end{figure}

The tree diagram presented in Figure~\ref{fig:tree3} can be interpreted as a single tree or as a combination of three smaller trees, distinguishable by their colors: one black, one orange, and another black.

Starting at the bottom of the rightmost black tree, there's the word \mention{you}. \mention{You}, a pronoun, is a specific type of noun, so it's labeled with an N. This pronoun, \mention{you}, functions as the head of an NP.

Jumping to the leftmost black tree, \mention{who} is another pronoun and is similarly labeled. It functions as the head of its NP.

The orange tree is centered around the word \mention{helped}, a verb. Its verb status is evident from its past-tense form, a feature exclusive to verbs. This verb is the head of a VP, analogous to how the noun is the head of an NP. The VP, in turn, is the head of the whole clause.

Each of the NPs functions as a dependent; the \mention{who} NP is a dependent in the clause, while the \mention{you} NP is a dependent in the VP. But we can consult Section~\ref{sec:Dep} to see more precisely what kind of dependent each is. \mention{You} is an NP in a VP, and it's you who is being helped, which is to say, being acted upon. And if we make the sentence passive (\mention{you were helped}), \mention{you} becomes the subject. All of which is to say, it has all the characteristics of an object.

We can go through the same process with \mention{who}. It's an NP at the start of a clause, and it denotes the person doing the helping. It's not really the topic, and we can't check agreement because the verb is not present tense, but it seems like \mention{who} must be the subject.

To ensure the accuracy of the tree diagram, I'll apply the checks from Section~\ref{sec:checkTrees}. The topmost label should be a phrasal category like \mention{Clause}, \textit{NP}, or \textit{VP} without any function \ding{51}. Each branch should culminate in a single word at its base \ding{51}. As we trace up a branch, we should encounter a label indicating \mention{head X}, where X is a lexical category \ding{51}. Continuing upwards, the next label should indicate a function and then a phrasal category \ding{51}. This pattern should persist until reaching the topmost label.

\subsubsection*{A note on tree styles}

Linguists have many different ways to draw syntax trees. You can find trees of the style I present here in \textit{CGEL} or in \mention{A student's introduction to English grammar} \citep{huddleston2021student}. If you want to search online for more examples, be sure to include \textit{CGEL} in your search query. Otherwise, you could end up with something rather different.

\subsection{A top-down example}
\is{syntax tree!building|(}

Let's try creating a syntax tree for the clause \mention{I had my breakfast}. We'll start at the top and work down and left to right. So, since this is a clause, we put that at the top:
\par
{
\centering
Clause
\par
}

Now, let's work left to right. \mention{I} is a subject NP, so we add a branch sloping down and to the left from the Clause label. The function goes above the category, so Subj: goes on the first line and NP below it.

\begin{figure}[H]
    \centering
    \includegraphics{figures/ihadmybreakfast.pdf}
    \label{fig:breakfast1}
\end{figure}

This NP needs a head, so we add a branch going straight down and label it Head:N\textsubscript{\term{pro}}.

\begin{figure}[H]
    \centering
    \includegraphics{figures/ihadmybreakfast2.pdf}
    \label{fig:breakfast2}
\end{figure}

And finally, we add \mention{I}.

\begin{figure}[H]
    \centering
    \includegraphics{figures/ihadmybreakfast3.pdf}
    \label{fig:breakfast3}
\end{figure}

Next, we'll add the Head:VP on a rightwards sloping branch from the Clause.

\begin{figure}[H]
    \centering
    \includegraphics{figures/ihadmybreakfast4.pdf}
    \label{fig:breakfast4}
\end{figure}

This VP has a head:V, which terminates in \mention{had}. There's also the object NP \mention{my breakfast}, so the branch from the VP should slope leftwards.

\begin{figure}[H]
    \centering
    \includegraphics{figures/ihadmybreakfast5.pdf}
    \label{fig:breakfast5}
\end{figure}

Finally, we'll add the object:NP \mention{my breakfast}, with its Det:DP \mention{my} and Head:N \mention{breakfast}.

\begin{figure}[H]
    \centering
    \includegraphics{figures/ihadmybreakfast6.pdf}
    \label{fig:breakfast6}
\end{figure}

Some people prefer to start with the sentence at the bottom of the page and work up through the lexical category head, the phrasal category and its function, etc. Play with both ways and see what works for you.

\begin{tblsframedsymbol}{Try it yourself}{pencil}
Practice the top-down approach with this sentence: \mention{She likes chocolate}. Start with \enquote{Clause} at the top, then work left to right. Remember: (1) Subject NP on the left, (2) Head VP on the right, (3) Each phrase needs its own head, (4) Every word sits at the bottom of a branch.
\end{tblsframedsymbol}





\subsection{Some trees}

Let's start with a very simple tree (Figure~\ref{fig:happy}). We'll start with a single-branching AdjP composed only of a Head.
\is{adjective, adjective phrase (AdjP)|(}
\begin{itemize}[noitemsep]
    \item As always, the top node is a phrasal category, in this case AdjP. There's no function because functions are relational, and this AdjP is not in a relation.
    \item The very bottom of our tree is a word node, in this case \mention{happy}.
    \item Immediately above \mention{happy} is a node with a function labeled Head. This is the function of \mention{happy} in the AdjP. It also has the category label Adj, which tells us that \mention{happy} is an adjective.
\end{itemize}

\begin{figure}[ht]
    \centering
    \includegraphics{figures/happy.pdf}
    \caption{Tree for \mention{happy}}
    \label{fig:happy}
\end{figure}
Let's try another one (Figure~\ref{fig:very}). This time we have a single-branching AdvP.
\is{adverb, adverb phrase (AdvP)|(}
\begin{itemize}[noitemsep]
    \item As always, the top node is a phrasal category, in this case AdvP. There's no function because functions are relational, and this AdvP is not in a relation.
    \item The very bottom of our tree is a word node, in this case \mention{very}.
    \item Immediately above \mention{very} is a node with a function labeled Head. This is the function of \mention{very} in the AdvP. It also has the category label Adv, which tells us that \mention{very} is an adverb.
\end{itemize}

\begin{figure}[ht]
    \centering
    \includegraphics{figures/very.pdf}
    \caption{Tree for \mention{very}}
    \label{fig:very}
\end{figure}
Let's put these together in Figure~\ref{fig:veryhappy}. This time we have a double-branching AdjP.
\begin{itemize}[noitemsep]
    \item As always, the top node is a phrasal category, in this case AdjP. There's no function because functions are relational, and this AdjP is not in a relation.
    \item If you start at the top and follow the right-hand branch, you see we have simply reproduced the AdjP from Figure~\ref{fig:happy}.
    \item On the left, though, we have the AdvP from Figure~\ref{fig:very}, but this time it's in a relation in the AdjP, so it has a function. Its function in the AdjP is Modifier.
\end{itemize}

\begin{figure}[ht]
    \centering
    \includegraphics{figures/veryhappy.pdf}
    \caption{Tree for \mention{very happy}}
    \label{fig:veryhappy}
\end{figure}
\is{adverb, adverb phrase (AdvP)|)}

This is the basis of all trees. Below the picture, we'll repeat this with the PP \mention{about it}. Before scrolling down, try drawing trees like Figures~\ref{fig:happy}--\ref{fig:veryhappy} for \mention{about}, \mention{it}, and \mention{about it}.

We'll try that again in Figure~\ref{fig:about}. We start with a single-branching PP.
\is{preposition, preposition phrase (PP)|(}
\begin{itemize}[noitemsep]
    \item As always, the top node is a phrasal category, in this case PP. There's no function because functions are relational, and this PP is not in a relation.
    \item The very bottom of our tree is a word node, in this case \mention{about}.
    \item Immediately above \mention{about} is a node with a function labeled Head. This is the function of \mention{about} in the PP. It also has the category label P, which tells us that \mention{about} is a preposition.
\end{itemize}

\begin{figure}[ht]
    \centering
    \includegraphics{figures/about.pdf}
    \caption{Tree for \mention{about}}
    \label{fig:about}
\end{figure}
This time we have a single-branching NP (Figure~\ref{fig:it}). Remember that pronouns are a special kind of noun.
\is{noun, noun phrase (NP)|(}
\begin{itemize}[noitemsep]
    \item As always~-- and by now you can probably finish this sentence for me~-- the top node is a phrasal category, in this case NP. There's no function because functions are relational, and this NP is not in a relation.
    \item The very bottom of our tree is a word node, in this case \mention{it}.
    \item Immediately above \mention{it} is a node with a function labeled Head. This is the function of \mention{it} in the NP. It also has the category label N, which tells us that \mention{it} is a noun (specifically a pronoun).
\end{itemize}

\begin{figure}[ht]
    \centering
    \includegraphics{figures/it.pdf}
    \caption{Tree for \mention{it}}
    \label{fig:it}
\end{figure}
This time we have a double-branching PP.
\begin{itemize}[noitemsep]
    \item As always, the top node in Figure~\ref{fig:aboutit} is a phrasal category, in this case PP. There's no function because functions are relational, and this PP is not in a relation.
    \item If you start at the top and follow the left-hand branch, you see we have simply reproduced the PP from Figure~\ref{fig:about}. Note that in Figure~\ref{fig:veryhappy} the Head was on the right and here it's on the left. There's no rule about which side the Head goes on. It depends on which dependents we find.
    \item On the right, though, we have the NP from Figure~\ref{fig:it}, but this time it's in a relation in the PP, so it has a function. Its function in the PP is Complement. (More specifically, it is an object.)
\end{itemize}

\begin{figure}[ht]
    \centering
    \includegraphics{figures/aboutit.pdf}
    \caption{Tree for \mention{about it}}
    \label{fig:aboutit}
\end{figure}
\is{preposition, preposition phrase (PP)|)}\is{noun, noun phrase (NP)|)}

Now we'll try putting Figures~\ref{fig:veryhappy} and~\ref{fig:aboutit} together in the AdjP \mention{very happy about it} (Figure~\ref{fig:veryhappyaboutit}). Try drawing this before scrolling down to check. Here's our final tree, the AdjP \mention{very happy about it}.
\begin{itemize}[noitemsep]
    \item As always, the top node is a phrasal category, in this case AdjP. There's no function because functions are relational, and this AdjP is not in a relation.
    \item If you start at the top and follow the middle branch, you see we have simply reproduced the AdjP from Figure~\ref{fig:happy}, and the leftmost two branches are just the AdjP from Figure~\ref{fig:veryhappy}.
    \item The right-hand branch is the PP from Figure~\ref{fig:aboutit}. But this time it's in a relation in the AdjP, so it has a function. Its function in the AdjP is Complement.
    \item Notice that there is a complement inside a complement. This property of being able to put one thing inside a thing of the same kind is called \term{recursion} (or ``nesting''). \is{recursion|(} It is the reason you can say \mention{the dog that chased the cat that ate the rat that lived in the house that Jack built}, and also the reason you probably shouldn't.
\end{itemize}

\begin{figure}[ht]
    \centering
    \includegraphics{figures/veryhappyaboutit.pdf}
    \caption{Tree for \mention{very happy about it}}
    \label{fig:veryhappyaboutit}
\end{figure}
\is{recursion|)}\is{syntax tree!building|)}
\is{adjective, adjective phrase (AdjP)|)}

\subsection{Identifying constituents}\label{sec:identifying_constituents}
\is{constituency tests|(}

When drawing more complex syntax trees, it can be challenging to identify the \emph{constituents} of your sentence. Constituents are groups of words that function as a single unit within the phrase structure. Determining what constitutes a constituent is crucial because it informs how you will structure your tree.

To identify constituents, you can use several \emph{constituency tests}. Here are the four primary tests you can apply:

\begin{enumerate}[noitemsep]
    \item Coordination test
    \is{coordination!test|(}
    \begin{itemize}[noitemsep]
        \item Explanation: If a group of words can be coordinated (joined with a conjunction like \mention{and} or \mention{or}) with a similar group, it's likely a constituent.
        \item Example:
        \begin{itemize}[noitemsep]
            \item Original: My well-meaning aunt gave all her loyalty cards \emph{to the kind man who helped you}.
            \item Coordination: My well-meaning aunt gave all her loyalty cards \emph{to the kind man who helped you} and \emph{to the woman at the counter}.
        \end{itemize}
        \item Analysis: The ability to coordinate \mention{to the kind man who helped you} with \mention{to the woman at the counter} suggests that it's a constituent.
    \end{itemize}
    \is{coordination!test|)}

    \item Pro-form replacement test
    \is{pro-form!replacement test|(}
    \begin{itemize}[noitemsep]
        \item Explanation: If a group of words can be replaced by a single pro-form (like \mention{he}, \mention{it}, \mention{there}), or \mention{do so}, it's likely a constituent.
        \item Example:
        \begin{itemize}[noitemsep]
            \item Original: My well-meaning aunt gave \emph{all her loyalty cards} to the kind man who helped you.
            \item Replacement: My well-meaning aunt gave \emph{them} to the kind man who helped you.
        \end{itemize}
        \item Analysis: Replacing \mention{all her loyalty cards} with \mention{them} indicates that it's a constituent.
    \end{itemize}
    \is{pro-form!replacement test|)}
    
    \item Ellipsis test
    \is{ellipsis!test|(}
    \begin{itemize}[noitemsep]
        \item Explanation: If a group of words can be omitted without making the sentence ungrammatical (and the meaning remains clear), it's likely a constituent.
        \item Example:
        \begin{itemize}[noitemsep]
            \item Original: My well-meaning aunt gave all her loyalty cards to the kind man who helped you, and her friend did too.
            \item Ellipsis: The omitted portion corresponds to \mention{gave all her loyalty cards to the kind man who helped you}.
        \end{itemize}
        \item Analysis: The omission of the repeated phrase suggests that it functions as a constituent.
    \end{itemize}
    \is{ellipsis!test|)}
    
    \item Dislocation (preposing) test
    \is{preposing|(}
    \begin{itemize}[noitemsep]
        \item Explanation: If a group of words can be moved to the beginning of a sentence without rendering it ungrammatical, it's likely a constituent.
        \item Example:
        \begin{itemize}[noitemsep]
            \item Original: My well-meaning aunt gave all her loyalty cards \emph{to the kind man who helped you}.
            \item Dislocation: \emph{To the kind man who helped you}, my well-meaning aunt gave all her loyalty cards.
        \end{itemize}
        \item Analysis: Moving \mention{to the kind man who helped you} to the front of the sentence suggests it's a constituent.
    \end{itemize}
    \is{preposing|)}
\end{enumerate}

By applying these tests, you can determine how to break down the sentence into its constituent parts before drawing your syntax tree.

\begin{tblsframedsymbol}{Practice: constituency testing}{pencil}
Apply the constituency tests to this sentence: \mention{The teacher explained the difficult concept clearly}. Test whether \mention{the difficult concept} is a constituent by trying: (1) Coordination: \textit{...explained \emph{the difficult concept} and \emph{the easy one}}, (2) Pro-form replacement: \textit{...explained \emph{it}}, (3) Can it be moved to the front?, (4) Can it be omitted in ellipsis?
\end{tblsframedsymbol}
\is{constituency tests|)}

\subsection{Checking your trees}\label{sec:checkTrees}
\is{syntax tree!well-formedness checks|(}

After you draw a tree, you should complete the following seven checks.

\begin{enumerate}[noitemsep]
    \item The very top label is a phrasal category (e.g., Clause, VP, NP, etc.) with no function.
    \item No matter how many branches go down from that label, one and only one is labeled Head.
    \item At the very bottom of each branch, there is a single word (e.g., \mention{jump}, \mention{ball}, \mention{the}, etc.).
    \item If you trace that branch up from that word, you run into a label that says Head:X.
    \item That X is a lexical category (e.g., N, V, Adj, D, etc.), not a phrasal category.
    \item If you follow that branch up, the next label is \term{Function}:XP (e.g., Subj:NP, Mod:AdvP, Head:VP, etc.).
    \item This is true all the way up until the very top label, which doesn't include a function (see 1.).
\end{enumerate}

Try these checks on Figures~\ref{fig:tree1} and~\ref{fig:tree2}. It's possible, though unlikely, that I introduced an error there.

\begin{tblsframedsymbol}{Self-assessment checklist}{test}
Before continuing, ensure you can confidently: Read a syntax tree from bottom to top; Identify heads and dependents in any phrase; Explain why we use function labels; Apply at least two constituency tests; Use the seven-point checklist to verify tree accuracy. If you can't confidently do all of these, review the relevant sections.
\end{tblsframedsymbol}
\is{syntax tree!well-formedness checks|)}

\section{More trees}\label{sec:more trees}

We'll now try some slightly more complex trees, using \mention{very happy about it}. First, we'll look at a simple clause: \mention{We won} (Figure~\ref{fig:wewon}).
\begin{itemize}[noitemsep]
    \item At the top, as always, we have a phrasal category label, in this case, Clause. Despite not calling it a \enquote{Clause Phrase}, it is still a phrasal unit, consisting of a Head and any dependents.
    \item The Head of the clause is the VP, which, in turn, has the verb \mention{won} as its Head.
    \item The dependent in the Clause is the Subject, on the left. It's an NP headed by the (pro)noun \mention{we}.
\end{itemize}

\begin{figure}[ht]
    \centering
    \includegraphics{figures/wewon.pdf}
    \caption{Tree for \mention{We won}}
    \label{fig:wewon}
\end{figure}
Next, we will change \mention{won} to the auxiliary verb \mention{are}, and add the complement \mention{very happy about it}.
\begin{itemize}[noitemsep]
    \item This tree takes the basic form of Figure~\ref{fig:happy}. The verb has changed from \mention{won} to \mention{are}, and we have added a complement.
    \item The complement is simply the AdjP from the previous section \mention{very happy about it}. The only difference is that there it wasn't in a relation, so it had no function label. Now it is the Complement of the VP, and its label reflects that.
\end{itemize}

\begin{figure}[ht]
    \centering
    \includegraphics{figures/weareveryhappyaboutit.pdf}
    \caption{Tree for \mention{We are very happy about it}}
    \label{fig:weareveryhappyaboutit}
\end{figure}
Sometimes, linguists will use a triangle (or a \enquote{coat hanger}) when they don't want to show the internal detail, usually because they don't have room on the page or because they want to focus on something else in the tree. We could, for example, replace the AdjP tree from Figure~\ref{fig:weareveryhappyaboutit} with a triangle, as in Figure~\ref{fig:weareveryhappyaboutit2}.

\begin{figure}[ht]
    \centering
    \includegraphics{figures/weareveryhappyaboutit2.pdf}
    \caption{Tree for \mention{We are very happy about it} with triangle}
    \label{fig:weareveryhappyaboutit2}
\end{figure}

\section{Disambiguating with trees}
\is{ambiguity resolution with trees|(}\is{attachment ambiguity|(}

A clause can be ambiguous, but a tree will usually disambiguate it by showing the structure. Consider \mention{Sherlock saw the man with the binoculars} in Figure~\ref{fig:binoculars}. Out of context, this is ambiguous between a sighting via the binoculars and a sighting of a man equipped with the binoculars.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/binoculars.png}
    \caption{Sherlock using binoculars to look at a man and Sherlock looking at a man who has binoculars}
    \label{fig:binoculars}
\end{figure}

We can show the two meanings with trees. In Figure~\ref{fig:binoculars1}, the PP \mention{with the binoculars} is a modifier in the VP; it explains how the seeing was accomplished. In Figure~\ref{fig:binoculars2}, the PP \mention{with the binoculars} is a modifier in the NP; it tells us about the man.

\begin{figure}[ht]
    \centering
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/binoculars1.pdf}
        \caption{VP modifier reading}
        \label{fig:binoculars1}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/binoculars2.pdf}
        \caption{NP modifier reading}
        \label{fig:binoculars2}
    \end{subfigure}
    \caption{Two trees for \mention{Sherlock saw the man with the binoculars}}
    \label{fig:binoculars-trees}
\end{figure}

\begin{tblsframedsymbol}{Practice: disambiguating with trees}{pencil}
Try this ambiguous sentence: \mention{I saw the student with the telescope}. Draw two different trees to show: (1) I used a telescope to see the student, (2) The student had a telescope. Where does the PP \mention{with the telescope} attach in each interpretation?
\end{tblsframedsymbol}
\is{attachment ambiguity|)}\is{ambiguity resolution with trees|)}

\section{Do we need the function labels?}
\is{function, syntactic!labels in trees|(}\is{adjunct|(}

Many linguists label only the categories, not the functions. This certainly produces less cluttered trees, but for language teachers, it's useful to be able to state the function of each constituent. It's also less clear if we omit the functions. Consider the difference between \mention{Brett and Michael, try harder} and \mention{Brett and Michael try harder}. The first one is an instruction to Brett and Michael, who have, it seems, been slacking off. The second is a positive statement about Brett and Michael's work ethic. If you avoid the function labels, the two sentences would have the same tree (Figure~\ref{fig:b+mtryharder}).

\begin{figure}[ht]
    \centering
    \includegraphics{figures/b+mtryharder.pdf}
    \caption{Tree for \mention{Brett and Michael try harder} without function labels}
    \label{fig:b+mtryharder}
\end{figure}
But when we add function labels, we can identify whether Brett and Michael is a subject (\mention{Brett and Michael try harder.} Figure~\ref{fig:b+mtryharder1}) or an adjunct (\mention{Brett and Michael, try harder!} Figure~\ref{fig:b+mtryharder2}).\footnote{Adjunct is a function we haven't talked about yet, but it means roughly something that is stuck on. In this case it's calling out to Brett and Michael, like saying ``hey''.}

\begin{figure}[ht]
    \centering
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/b+mtryharder1.pdf}
        \caption{Subject reading}
        \label{fig:b+mtryharder1}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/b+mtryharder2.pdf}
        \caption{Adjunct reading}
        \label{fig:b+mtryharder2}
    \end{subfigure}
    \caption{Function labels disambiguate \mention{Brett and Michael try harder} and \mention{Brett and Michael, try harder!}}
    \label{fig:b+mtryharder-pair}
\end{figure}
\is{adjunct|)}\is{function, syntactic!labels in trees|)}

\section{Some terminological issues} \label{sec:terminology}
\is{category, linguistic!vs function|(}\is{adverbial (terminology)|(}

You will often see expressions like \enquote{adverbial}, \enquote{adverb phrase}, or \enquote{adverb clause} used when there isn't a single adverb to be found. This is based on an entirely semantic idea of what it means to be \enquote{adverbial}. Specifically, if something answers the questions \enquote{when}, \enquote{where}, \enquote{why}, or \enquote{how}, then many grammarians use the term \term{adverbial}.

They're not wrong.

Terminology is arbitrary. There's no inherently correct term. We could use a different term for what we're calling \term{determiners}. We could call them \term{specifiers} instead. Or we could call them \term{articles} or \term{markers} or \term{spoons} or \term{category 6}. In principle, any label should do.

We could even call them \term{Steve}.\footnote{Let's call it ``Steve''. \url{https://www.youtube.com/embed/CiGFRLCC-Ao}}

BUT.

While the label does not matter, the way we constitute the category does. And a category of things that \enquote{answer the question `when', `where', `why', or `how}', based entirely on semantics, is not a useful syntactic category. By this definition, \mention{my birthday} is \enquote{adverbial}. So are \mention{Toronto}, \mention{reasons}, and \mention{with binoculars}. But these things just don't go together. They don't make a coherent set, and you can't teach anybody anything useful about grammar by lumping them together.

Terms are just labels, and any label will work in principle.\footnote{In practice, if you use a highly eccentric label, obviously you will not be understood by many people. Recall the discussion of standards in Section~\ref{sec:standards}.} But categories matter. They tell us about the world. And trying to make sense of badly constructed categories just makes life harder for everyone.
\is{adverbial (terminology)|)}\is{category, linguistic!vs function|)}

\section{Summary}

If you can draw a tree, you can't hide from your own analysis. Every node forces a commitment~-- this word belongs to this category, this phrase serves this function~-- and the result is either right or visibly wrong. The next time a student's sentence puzzles you, sketch the tree. The structure will show you where the difficulty is, often before you've finished drawing.
\is{syntax tree|)}
